# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local configuration (see .env.template, which IS tracked)
.env

# jai-proxy output archive -- only used when JAI_PROXY_OUTPUT_DIR is unset
/cards/

# The archive itself. Lives in-tree so dev sees the same layout the
# container sees (./data/...), but is never committed.
/data/*
!/data/.gitkeep

# Server working state: definition captures + lorebook cache
/state/

# datacat PNGs staged for `make import`
/import/

# Editors
.idea/
.vscode/

# macOS
.DS_Store

# pytest
.pytest_cache/

# node (userscript/tests/)
node_modules/
